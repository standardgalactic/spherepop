/* topology.h — TOPOLOGY subsystem interface. */
#ifndef SP_TOPOLOGY_H
#define SP_TOPOLOGY_H
/* Interface stubs — see topology.c for implementation notes. */
#include <stdbool.h>
#include <stdint.h>
#endif

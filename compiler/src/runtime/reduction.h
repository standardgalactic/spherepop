/* reduction.h — REDUCTION subsystem interface. */
#ifndef SP_REDUCTION_H
#define SP_REDUCTION_H
/* Interface stubs — see reduction.c for implementation notes. */
#include <stdbool.h>
#include <stdint.h>
#endif

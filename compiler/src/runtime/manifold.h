/* manifold.h — MANIFOLD subsystem interface. */
#ifndef SP_MANIFOLD_H
#define SP_MANIFOLD_H
/* Interface stubs — see manifold.c for implementation notes. */
#include <stdbool.h>
#include <stdint.h>
#endif

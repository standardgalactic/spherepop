/* semantic_fields.h — Semantics: semantic fields. See §D–F. */
#ifndef SP_SEM_SEMANTIC_FIELDS_H
#define SP_SEM_SEMANTIC_FIELDS_H
/* Interface stubs — category/sheaf machinery to be layered on
 * runtime core once bubble and history types are stable. */
#endif

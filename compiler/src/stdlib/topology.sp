/* topology.sp — Topological operations on bubble structures.
 *
 * These functions operate on histories and bubble references to
 * compute topological invariants. Interface is stable; implementation
 * deepens as the runtime matures. See §24.
 */
fn bubble_depth_measure(h) {
    /* Returns a measure of nesting depth from history */
    return h
}

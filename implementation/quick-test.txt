sp-init
sp-overlay-new t
a=$(sp-pop-overlay t | awk '{print $2}')
b=$(sp-pop-overlay t | awk '{print $2}')
sp-link-overlay t "$a" "$b" relates

# preview snapshot & diffs on the speculative branch
spsnap --overlay t
spdiff --overlay t

